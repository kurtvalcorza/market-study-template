{
  "name": "{{STUDY_SLUG}}",
  "version": "0.1.0",
  "private": true,
  "description": "{{STUDY_TITLE}}: instrument, analysis, and report",
  "type": "module",
  "scripts": {
    "build": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "lint": "eslint . --ext .ts,.tsx",
    "lint:report": "python tools/report-check.py REPORT.md chapters/*.qmd --strict",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "eslint": "^9.0.0",
    "typescript": "^5.6.0",
    "vitest": "^3.0.0",
    "@vitest/coverage-v8": "^3.0.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
